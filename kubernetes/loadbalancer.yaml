apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
spec:
  type: LoadBalancer                  # AWS ELB will be created
  selector:
    app: fastapi
  ports:
    - protocol: TCP
      port: 80                        # Public port
      targetPort: 8000                # Container port
